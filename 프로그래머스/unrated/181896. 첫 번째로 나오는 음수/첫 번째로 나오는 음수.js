function solution(num_list) {
    let result = 0
    for (let i = 0; i < num_list.length; i++) {
        if (num_list[i] > 0) {
            result = -1}
        else {
           return result = num_list.indexOf(num_list[i])
        }
    }
    return result
}